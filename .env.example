# URL pública del backend a la que llama el navegador.
# Para desarrollo local con docker compose, dejar en localhost:8000.
# En la VM del hospital, cambiar por la dirección real, por ejemplo:
#   NEXT_PUBLIC_API_URL=http://10.0.0.20:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
